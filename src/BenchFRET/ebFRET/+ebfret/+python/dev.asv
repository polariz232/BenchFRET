nstates = 10;
ntraces = 10;
trace_length = 300;

data = {};
iter = 1;

for state = 2:nstates
    for n = 1:ntraces

        dat = {};
        dat{end+1} = 4 * ones(trace_length,1);
        dat{end+1} = 3 * ones(trace_length,1);
        dat{end+1} = 3 * ones(trace_length,1);
        dat{end+1} = 3 * ones(trace_length,1);

    end
end